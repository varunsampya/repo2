import sys
import os

# Get the name and bundle name from the command line arguments.
testname = sys.argv[1]
bundle_name = sys.argv[2]

# Print the name and bundle name.
print(testname)
print(bundle_name)

